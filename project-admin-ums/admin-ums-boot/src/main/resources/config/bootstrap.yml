server:
  port: 8091
spring:
  application:
    name: @project.name@
  profiles:
    active: @env@
build-info:
  app-name: @project.name@
  app-version: @project.version@
app:
  log:
    path: works/logs